/*
 * Github
 */
// Init Khoi tao
$ git init

// Add file
$ git add -A // update all file

// Status Xem tinh trang
$ git status // xem tinh trang

// Commit file
$ git commit -m "update more file"

// Push file
$ git push -u origin <main>
$ git push -u origin <dev>

// Pull keo file ve
$ git pull


/*
 *  khai báo hàm
 */
=1 float f_thi_du(float x, int b, int c)
=2 kiểu_dữ_liệu tên hàm(các tham số hình thức và kiểu của chúng) 

/*
 * Cấu trúc chương trình
 */

#include <thư viện.h>
using namespace std;

// -> Chương trình chính
// Cach 1 -> Dinh nghĩa hàm chính void
void main()
{
    // Prototype hàm con
    void ham_con_1(kiểu giá trị 1, kiểu giá trị 2)
    void ham_con_2(kiểu giá trị 1, kiêu giá trị 2)
    // Không cần return khi định nghĩa kiểu giá trị hảm == void
}

// Phần cuối chương trình
void ham_con_1(kiểu giá trị 1 & tên giá trị, kiểu giá trị 2 & tên giá trị 2)
void ham_con_2(kiểu giá trị 1 & tên giá trị, kiểu giá trị 2 & tên giá trị 2)

// Cach 2 -> ĐỊnh nghĩa hàm chín Int trả về giá trị int cho hàm main
// cần return 0; ở cuối cùng

// Cach 3 -> include > Din nghia ham full > void main khong return gia tri, int main return gia tri


/*
 * Bien toan cuc, truoc main
 */

/*
 * gcc
 */
 $ g++ -g HelloWorld.cpp -o test
 Save trước rồi build g++ lại.

 --> Luôn luôn Push code cuôi ngày
 --> Pull code đầu ngày


